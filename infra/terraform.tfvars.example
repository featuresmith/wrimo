cloudflare_account_id = "00000000000000000000000000000000"
cloudflare_api_token  = "cf-api-token"
worker_name           = "wrimo"
worker_script = <<-EOT
addEventListener("fetch", event => {
  event.respondWith(new Response("Wrimo Worker placeholder", { status: 200 }));
});
EOT
worker_compatibility_date = "2024-05-01"

worker_secrets = {
  # EXAMPLE_KEY = "value"
}

# Zone that owns the Worker domains. Required when custom_domains is non-empty.
cloudflare_zone_id = "00000000000000000000000000000000"

# Provide hostnames to bind to the Worker. Set to [] or null to disable.
custom_domains = [
  "wrimo.io",
  # "www.wrimo.io",
]
