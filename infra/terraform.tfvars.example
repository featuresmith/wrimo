cloudflare_account_id = "00000000000000000000000000000000"
cloudflare_api_token  = "cf-api-token"
worker_name           = "wrimo"
worker_compatibility_date = "2024-10-11"

worker_secrets = {
  # EXAMPLE_KEY = "value"
}

# Zone that owns the Worker domains. Required when custom_domains is non-empty.
cloudflare_zone_id = "00000000000000000000000000000000"

# Provide hostnames to bind to the Worker. Set to [] or null to disable.
custom_domains = [
  "wrimo.io",
  # "www.wrimo.io",
]

# Override these only if the Worker module or build output live in different locations.
# worker_main_module_name = "index.js"
# worker_main_module_path = "../worker/index.js"
# worker_assets_directory = "../dist"
